import React from "react";
import "./Loading.scss";

const Loading = () => {
  return (
    <div data-testid="loading-element" className="loading">
      LOADING
    </div>
  );
};

export default Loading;
